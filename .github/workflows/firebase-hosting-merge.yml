# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
"on":
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        # Install dependencies (recommended if you haven't done so)
      - name: Install dependencies
        run: npm ci

        # Explicitly install Vite
      - name: Explicitly install Vite
        run:
          npm install vite@latest --save-dev

          # Verify Vite installation
      - name: Check Vite installation
        run: npx vite --version
      - run: npm run build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: "${{ secrets.GITHUB_TOKEN }}"
          firebaseServiceAccount: "${{ secrets.FIREBASE_SERVICE_ACCOUNT_STREAMGPT_3438B }}"
          channelId: live
          projectId: streamgpt-3438b
